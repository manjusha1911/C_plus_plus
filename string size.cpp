#include<iostream>
#include<string>
using namespace std;
int main(){
    string a="om sai ram";
    cout<<a.length();
    return 0;
}



/* You might see some C++ programs that use the size() function
 to get the length of a string. This is just an alias of length(). It is completely up to you 
 if you want to use length() or size():

